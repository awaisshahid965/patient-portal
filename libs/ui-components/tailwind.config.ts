import type { Config } from 'tailwindcss'
// import config from '../../libs/ui-components/tailwind.config'

const config: Config = {
    content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
    theme: {
        extend: {},
    },
    plugins: [],
}
export default config
